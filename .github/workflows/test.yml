# .github/workflows/test.yml
name: Test Workflow

on:
  push:
    branches:
      - master

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Say Hello Github
        run: echo "Hello from GitHub Actions!"
